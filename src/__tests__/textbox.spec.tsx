import React from "react";
import { shallow } from "enzyme";

describe("textbox", () => {
    it("should allow generation of a simple textbox form", () => {
        const component = shallow(
            <div>Testing</div>
        );
    });
});
